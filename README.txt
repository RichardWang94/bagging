Richard Wang
python bagit [-v] T size <train> <test>
where –v is an optional “verbose” flag, 
T is number of classifiers (and bootstrap sample sets) created, 
size is the number of data points in each bootstrap sample set,
and <train> and <test>are filenames containing the training and testing data sets,respectively. 
T and size can be any non-zero integers. 
